# compiled binary
/slqs

# secrets / tokens / profiles / local cache
profiles.json
*.token
*secret*
.env
*.db
*.sqlite

# nix
result
result-*
.direnv/

# vendored UI (synced from canonical slk-gui-proto via sync-ui.sh): the QML is
# committed, but the daemon writes these into the UI dir at runtime — keep them out.
ui/channels.json
ui/codemap.json
ui/emoji.json
ui/emoji-*.json
ui/*.log
